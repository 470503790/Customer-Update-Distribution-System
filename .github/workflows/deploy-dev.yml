name: Deploy-Dev

on:
  push:
    branches: [ develop ]

env:
  DEPLOY_SSH_HOST_DEV: ${{ secrets.DEPLOY_SSH_HOST_DEV }}
  DEPLOY_SSH_USER_DEV: ${{ secrets.DEPLOY_SSH_USER_DEV }}
  DEPLOY_SSH_KEY_DEV: ${{ secrets.DEPLOY_SSH_KEY_DEV }}
  DEPLOY_PATH_DEV: ${{ secrets.DEPLOY_PATH_DEV }}

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Login GHCR (placeholder)
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Build & Push image (placeholder)
        run: |
          echo "TODO: docker build and push dev image"
      - name: Deploy via SSH (placeholder)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.DEPLOY_SSH_HOST_DEV }}
          username: ${{ env.DEPLOY_SSH_USER_DEV }}
          key: ${{ env.DEPLOY_SSH_KEY_DEV }}
          script: |
            echo "TODO: deploy to ${DEPLOY_PATH_DEV} with docker compose"
